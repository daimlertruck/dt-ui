import { radius } from './radius';

export const shape = {
  button: radius['none'],
  modal: radius['none'],
  emptyState: radius['none'],
  dropdown: radius['none'],
  message: radius['none'],
  progressbar: radius['none'],
};
