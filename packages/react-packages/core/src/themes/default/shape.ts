import { radius } from './radius';

export const shape = {
  button: radius['5xs'],
  modal: radius['5xs'],
  emptyState: radius['4xs'],
  dropdown: radius['5xs'],
  message: radius['6xs'],
  progressbar: radius['none'],
};
