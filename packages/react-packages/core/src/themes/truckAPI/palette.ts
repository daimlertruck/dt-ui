import { palette as defaultPalette, colors } from '../default/palette';

export const palette = {
  ...defaultPalette,
  primary: {
    default: colors.blue_60,
    light: colors.blue_10,
    medium: colors.blue_30,
    dark: colors.blue_80,
    contrast: colors.white,
  },
};
