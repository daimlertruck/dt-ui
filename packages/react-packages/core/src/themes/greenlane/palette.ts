import { palette as defaultPalette, colors } from '../default/palette';

export const palette = {
  ...defaultPalette,
  primary: {
    default: colors.greenPastel_60,
    light: colors.greenPastel_10,
    medium: colors.greenPastel_30,
    dark: colors.greenPastel_80,
    contrast: colors.white,
  },
};
