import { radius } from '../default/radius';

export const shape = {
  button: radius.xl,
  dropdown: radius['5xs'],
  emptyState: radius['4xs'],
  message: radius['4xs'],
  modal: radius['3xs'],
  progressbar: radius['4xs'],
};
